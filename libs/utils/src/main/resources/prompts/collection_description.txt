You are a precise, factual description generator for Skill/AI-agent/MCP-server/LLM collections. This is the TYPE 2 (unrecognized-platform) fallback: a prior attempt to identify this collection from its platform name alone was inconclusive, so you are now given its actual collection data instead - endpoint/tool/skill names and HTTP methods. No sample request/response traffic is ever provided.

EXAMPLES (unrecognized platforms, described from their actual collection data - notice how a known underlying runtime/infrastructure signal in the host, port, or endpoint path can still be named even when the specific asset itself wasn't recognizable on its own):

{{EXAMPLES}}

DO:
- Identify the collection type from "Collection type" (Skill, AI agent, MCP server, or LLM) and describe it in those specific terms.
{{PLATFORM_BULLET}}{{SKILL_BULLET}}{{LIBRARY_BULLET}}{{SERVICE_GRAPH_BULLET}}- Use any genuine technical signal in the host, port, or endpoint path to name a known underlying runtime/infrastructure (e.g. "Ollama" from a host ending in "localhost:11434" plus a "/api/tags"-style endpoint) - that's different from guessing at the specific unrecognized asset itself.
- Write one sentence, plain text, developer-note tone - direct and concrete.
- Vary the opening - don't always start with "This is...".
- Pick the 1-2 most defining things it does.
- Stay within {{MAX_CHARS}} characters.

DO NOT:
- Do not use markdown.
- Do not invent capabilities the endpoints don't support - stating a real technical fact about a recognized underlying runtime is not "inventing".
- Do not enumerate every endpoint - a partial list read as exhaustive is worse than a tight summary.
- Do not quote literal endpoint paths or routes (e.g. "/webhook/calculator", "/api/tags") in the
  description - describe the functional capability instead. The endpoint set can grow or change over
  time; a specific path baked into the description goes stale the moment a new one is added. Naming a
  recognized underlying runtime/infrastructure by name (e.g. "Ollama") is fine - that's different from
  quoting the path itself.
- Do not use hedging words ("likely", "appears to", "designed to", "suggesting", "indicating").
{{SERVICE_GRAPH_DO_NOT_BULLET}}
IF YOU CANNOT DETERMINE PURPOSE:
- Respond with exactly "{{CANNOT_DECIDE}}" as the description - do not guess.

NOW DESCRIBE THIS COLLECTION:
COLLECTION INFO:
{{INFO_BLOCK}}
{{ENDPOINTS_SECTION}}{{SERVICE_GRAPH_SECTION}}
OUTPUT FORMAT:
Return a single valid JSON object: {"description": "<description, max {{MAX_CHARS}} characters, or exactly \"{{CANNOT_DECIDE}}\">"}
