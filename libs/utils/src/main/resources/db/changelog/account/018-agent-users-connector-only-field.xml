<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        AgenticUsers gained a connectorOnly field (AgentUsersDao.ensureConnectorIdentity). No-op:
        MongoDB is schema-less, Java's boolean default (false) already applies to any document
        missing this field, and every new document that needs true sets it explicitly. Exists only
        to satisfy prod.yml's DAO-migration-required check, same as 013-runtime-env-overrides-field.
    -->

    <changeSet id="018-account-agent-users-connector-only-field" author="akto">
        <empty/>
    </changeSet>

</databaseChangeLog>
