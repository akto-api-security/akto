<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:mongodb="http://www.liquibase.org/xml/ns/mongodb"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
        http://www.liquibase.org/xml/ns/mongodb
        http://www.liquibase.org/xml/ns/mongodb/liquibase-mongodb-latest.xsd">

    <!--
        Collection + agentHost index for AgentGuardCorpusDao.createIndicesIfAbsent
        (collName: agent_guard_corpus, index on AgentGuardCorpusEntry.AGENT_HOST
        descending). Index name matches MCollection.generateIndexName
        ("agentHost_-1") so the DAO's createIndexIfAbsent treats it as present.
    -->

    <changeSet id="008-account-create-agent-guard-corpus" author="akto">
        <preConditions onFail="MARK_RAN">
            <not>
                <mongodb:collectionExists collectionName="agent_guard_corpus"/>
            </not>
        </preConditions>
        <mongodb:createCollection collectionName="agent_guard_corpus"/>
        <rollback>
            <mongodb:dropCollection collectionName="agent_guard_corpus"/>
        </rollback>
    </changeSet>

    <changeSet id="008-account-agent-guard-corpus-agent-host-index" author="akto">
        <preConditions onFail="MARK_RAN">
            <mongodb:collectionExists collectionName="agent_guard_corpus"/>
        </preConditions>
        <mongodb:createIndex collectionName="agent_guard_corpus">
            <mongodb:keys>{ "agentHost": -1 }</mongodb:keys>
            <mongodb:options>{ "name": "agentHost_-1" }</mongodb:options>
        </mongodb:createIndex>
        <rollback>
            <mongodb:dropIndex collectionName="agent_guard_corpus">
                <mongodb:keys>{ "agentHost": -1 }</mongodb:keys>
            </mongodb:dropIndex>
        </rollback>
    </changeSet>

</databaseChangeLog>
