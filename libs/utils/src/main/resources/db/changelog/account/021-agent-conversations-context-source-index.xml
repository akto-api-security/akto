<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:mongodb="http://www.liquibase.org/xml/ns/mongodb"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
        http://www.liquibase.org/xml/ns/mongodb
        http://www.liquibase.org/xml/ns/mongodb/liquibase-mongodb-latest.xsd">

    <!--
        Mirrors AgentConversationDao.createIndexIfAbsent. GenericAgentConversation
        gained a contextSource field so the Ask Akto chat-history listing can be
        scoped per product tab, and fetchHistory's aggregation now matches on it.
        Without this index, that match falls back to a full collection scan.

        Index name matches MCollection.generateIndexName so this is a no-op if
        DaoInit already built it.
    -->

    <changeSet id="021-account-agent-conversations-context-source-index" author="akto">
        <preConditions onFail="MARK_RAN">
            <mongodb:collectionExists collectionName="generic_agent_conversations"/>
        </preConditions>
        <mongodb:createIndex collectionName="generic_agent_conversations">
            <mongodb:keys>{ "contextSource": -1, "lastUpdatedAt": -1 }</mongodb:keys>
            <mongodb:options>{ "name": "contextSource-lastUpdatedAt_-1" }</mongodb:options>
        </mongodb:createIndex>
        <rollback>
            <mongodb:dropIndex collectionName="generic_agent_conversations">
                <mongodb:keys>{ "contextSource": -1, "lastUpdatedAt": -1 }</mongodb:keys>
            </mongodb:dropIndex>
        </rollback>
    </changeSet>

</databaseChangeLog>
