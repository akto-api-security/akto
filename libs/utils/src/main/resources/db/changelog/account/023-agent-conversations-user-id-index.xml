<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:mongodb="http://www.liquibase.org/xml/ns/mongodb"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
        http://www.liquibase.org/xml/ns/mongodb
        http://www.liquibase.org/xml/ns/mongodb/liquibase-mongodb-latest.xsd">

    <!--
        Mirrors AgentConversationDao.createIndexIfAbsent. GenericAgentConversation
        gained a userId field so the Ask Akto chat-history listing is scoped to the
        requesting user, and fetchHistory's aggregation now matches on it.

        Index name matches MCollection.generateIndexName so this is a no-op if
        DaoInit already built it.
    -->

    <changeSet id="023-account-agent-conversations-user-id-index" author="akto">
        <preConditions onFail="MARK_RAN">
            <mongodb:collectionExists collectionName="generic_agent_conversations"/>
        </preConditions>
        <mongodb:createIndex collectionName="generic_agent_conversations">
            <mongodb:keys>{ "userId": -1, "lastUpdatedAt": -1 }</mongodb:keys>
            <mongodb:options>{ "name": "userId-lastUpdatedAt_-1" }</mongodb:options>
        </mongodb:createIndex>
        <rollback>
            <mongodb:dropIndex collectionName="generic_agent_conversations">
                <mongodb:keys>{ "userId": -1, "lastUpdatedAt": -1 }</mongodb:keys>
            </mongodb:dropIndex>
        </rollback>
    </changeSet>

</databaseChangeLog>
