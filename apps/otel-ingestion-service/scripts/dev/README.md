#!/usr/bin/env bash
# Local-only Cowork replay helpers (gitignored via scripts/dev/).
# See ../../README.md for prerequisites.
set -euo pipefail
echo "Dev scripts live here. Example:"
echo "  ./scripts/dev/local-sync.sh [path/to/logs-otel-good.txt]"
