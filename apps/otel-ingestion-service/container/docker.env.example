# Copy to docker.env for local docker compose (docker.env is gitignored).
# cp docker.env.example docker.env

SERVER_PORT=8080
LOG_LEVEL=info
AKTO_OTLP_AUTHENTICATE=false
OTLP_LOG_SENSITIVE=false
OTLP_HTTP_SINK_ENABLED=false
DEFAULT_DATA_INGESTION_URL=http://host.docker.internal:8081
