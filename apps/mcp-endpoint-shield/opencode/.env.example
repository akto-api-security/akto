# Akto OpenCode Guardrails — Configuration Reference
# Copy relevant lines to your shell profile or .env loader.

# ==========================================
# REQUIRED
# ==========================================

# Akto guardrails/ingestion endpoint
AKTO_DATA_INGESTION_URL=https://your-akto-instance/guardrails

# ==========================================
# OPTIONAL: Authentication & Request
# ==========================================

# Bearer token for Akto API calls (sent as Authorization header)
# AKTO_API_TOKEN=

# Request timeout in seconds (default: 5)
# AKTO_TIMEOUT=5

# Run guardrails synchronously and block on result (default: true)
# AKTO_SYNC_MODE=true

# ==========================================
# OPTIONAL: Mode & Device
# ==========================================

# Operation mode: 'argus' (default) or 'atlas' (device-aware endpoint routing)
# MODE=argus

# Device ID for atlas mode (auto-generated from machine if not set)
# DEVICE_ID=

# ==========================================
# OPTIONAL: OpenCode API Host Override
# ==========================================

# OpenCode API URL (default: https://api.opencode.ai)
# Only change this if using a custom OpenCode endpoint; atlas mode derives the URL from DEVICE_ID automatically
# OPENCODE_API_URL=https://api.opencode.ai

# ==========================================
# OPTIONAL: Logging
# ==========================================

# Directory for hook log files (default: ~/.config/opencode/akto/logs)
# LOG_DIR=~/.config/opencode/akto/logs

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# LOG_LEVEL=INFO

# Log full request/response payloads — for debugging only
# LOG_PAYLOADS=false

# ==========================================
# OPTIONAL: Behaviour
# ==========================================

# Context source tag applied to mirrored traffic (default: ENDPOINT)
# CONTEXT_SOURCE=ENDPOINT
