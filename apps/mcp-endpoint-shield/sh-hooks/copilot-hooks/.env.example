AKTO_DATA_INGESTION_URL=http://localhost:80
AKTO_API_TOKEN=                             # Authorization header value sent to AKTO_DATA_INGESTION_URL

AKTO_TIMEOUT=5                              # Optional

GITHUB_COPILOT_API_URL=https://api.github.com   # Optional
VSCODE_API_URL=https://vscode.dev               # Optional

AKTO_SYNC_MODE=true
DEVICE_ID=                                  # Optional: overrides auto-detected machine id
AKTO_CONNECTOR=                             # Optional: overrides the non-VS Code fallback connector label (default copilot)

LOG_DIR=~/.copilot/akto/logs                # Optional: shared across both surfaces so prompt/response turns correlate
LOG_LEVEL=INFO                              # Optional
LOG_PAYLOADS=false                          # Optional: logs request/response payload previews

MCP_INGEST_PATH=/mcp                        # Optional
